namespace WpfMusicPlayer.Models
{
    public enum PlayMode
    {
        Normal,
        RepeatOne,
        RepeatAll,
        Shuffle
    }

    public enum PlaybackState
    {
        Stopped,
        Playing,
        Paused
    }
}
